<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add New Equipment</title>
  <%= stylesheet_link_tag 'application', media: 'all' %>
</head>
<body>
<header>
</header>

<main>
  <div class="overlay" id="overlay">
    <div class="overlay-content">
      <h1>Add New Equipment</h1>
      <%= form_with model: @equipment, url: equipments_path do |form| %>
        <div>
          <%= form.text_field :Equipment_Name, placeholder: "Equipment Name" %>
          <% @equipment.errors.full_messages_for(:Equipment_Name).each do |message| %>
            <div><%= message %></div>
          <% end %>
        </div>

        <div>
          <%= form.select :Financial_Record_Id, options_for_select(FinancialRecord.all.collect { |f| [f.Title, f.Financial_Record_ID] }), prompt: "Financial Record" %>
          <% @equipment.errors.full_messages_for(:Financial_Record_Id).each do |message| %>
            <div><%= message %></div>
          <% end %>
        </div>

        <div>
          <%= form.select :Club_ID, options_for_select(Club.all.collect { |c| [c.Club_Name, c.Club_ID] }), prompt: "Club" %>
          <% @equipment.errors.full_messages_for(:Club_ID).each do |message| %>
            <div><%= message %></div>
          <% end %>
        </div>

        <div>
          <%= form.select :Vendor_ID, options_for_select(Vendor.all.collect { |v| [v.Name, v.Vendor_ID] }), prompt: "Vendor (Optional)" %>
          <% @equipment.errors.full_messages_for(:Vendor_ID).each do |message| %>
            <div><%= message %></div>
          <% end %>
        </div>

        <div>
          <%= form.submit "Save Equipment", class: "button" %>
        </div>
      <% end %>

      <button onclick="history.back()">Cancel</button>
    </div>
  </div>
</main>
</body>
</html>
