<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Notification Details</title>
  <style>
    /* General Styling for Body */
    body {
      font-family: Arial, sans-serif;
      background-color: #072326;
      color: #e6f2ef;
      margin: 0;
      padding: 20px;
    }

    /* Page Title */
    h1 {
      font-size: 2em;
      font-weight: bold;
      margin-bottom: 20px;
      text-align: center;
      color: #d8f6e1;
    }

    /* Back Button Styling */
    .back-button-container {
      display: flex;
      justify-content: flex-start;
      width: 80%;
      max-width: 1000px;
      margin: 0 auto 10px auto;
    }

    .back-button {
      display: inline-block;
      padding: 10px 20px;
      font-size: 1em;
      text-align: center;
      color: #072326;
      background-color: #b2e3b4;
      border: 2px solid #CCF2C2;
      border-radius: 8px;
      text-decoration: none;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .back-button:hover {
      background-color: #d8f6e1;
      color: #072326;
    }

    /* Notification Details Wrapper */
    .details-wrapper {
      width: 80%;
      max-width: 1000px;
      margin: 0 auto 20px auto;
      background-color: #0a373a;
      padding: 20px;
      border-radius: 12px;
      border: 2px solid #CCF2C2;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .notification-details {
      flex-grow: 1;
    }

    .notification-details p {
      font-size: 1.1em;
      line-height: 1.6;
      margin: 10px 0;
    }

    /* Delivery History Section */
    h2 {
      font-size: 1.5em;
      font-weight: bold;
      color: #b2e3b4;
      text-align: center;
      margin: 30px 0;
    }

    /* History Table */
    table {
      width: 80%;
      max-width: 1000px;
      margin: 0 auto;
      border-collapse: collapse;
      background-color: #072326;
      border-radius: 12px;
      overflow: hidden;
      border: 2px solid #CCF2C2;
    }

    th, td {
      padding: 15px;
      text-align: left;
      border-bottom: 1px solid #CCF2C2;
    }

    th {
      background-color: #b2e3b4;
      color: #072326;
      font-weight: bold;
    }

    tr:nth-child(even) {
      background-color: #0f4648;
    }

    tr:hover {
      background-color: #d8f6e1;
      color: #072326;
    }
  </style>
</head>
<body>
<a href="<%= notifications_path %>" class="back-button">Back to Notifications History</a>

  <h1>Notification Details</h1>

  <!-- Back Button -->
  <div class="back-button-container">
  </div>

  <!-- Notification Details -->
  <div class="details-wrapper">
    <div class="notification-details">
      <p>Type: <%= @notification.notification_type %></p>
      <p>Message: <%= @notification.message %></p>
      <p>Status: <%= @notification.status %></p>
      <p>Triggered At: <%= @notification.triggered_at.strftime('%d/%m/%Y %H:%M') %></p>
    </div>
  </div>

  <h2>Delivery History</h2>
  <table>
    <thead>
      <tr>
        <th>Delivery Method</th>
        <th>Status</th>
        <th>Delivered At</th>
        <th>Failure Reason</th>
      </tr>
    </thead>
    <tbody>
      <% @histories.each do |history| %>
        <tr>
          <td><%= history.delivery_method %></td>
          <td><%= history.delivery_status %></td>
          <td><%= history.delivered_at&.strftime('%d/%m/%Y %H:%M') || "N/A" %></td>
          <td><%= history.failure_reason || "N/A" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</body>
</html>
