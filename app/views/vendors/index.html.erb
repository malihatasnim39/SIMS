<header>
  <%= image_tag 'back-icon.png', alt: 'Back', style: 'width: 20px; cursor: pointer;' %>
  <div class="search-bar">
    <%= form_with url: vendors_path, method: :get, local: true, class: "search-form" do %>
      <%= text_field_tag :query, params[:query], placeholder: "Search vendors...", class: "search-input" %>
  </div>
      <%= submit_tag "Search", class: "search-button" %>
    <% end %>
  <%= image_tag 'user-icon.png', alt: 'User', style: 'width: 20px; cursor: pointer;' %>
</header>

<div class="container">
  <div class="sort">
    <span>Sorted by Availability</span>
    <span>
      <%= image_tag 'filter-icon.png', alt: 'Filter', style: 'width: 20px;' %>
    </span>
  </div>
  <div class="cards">
    <!-- Loop through equipment items -->
    <% @vendors.each do |vendor| %>
      <div class="card">
        <div class="card-content">
          <div class="card-title"><%= vendor.Name %></div>
        </div>
        <%= link_to ">", vendor, class: "more-link" %>
      </div>
    <% end %>
    <!-- "+" card to open the modal -->
    <div class="card add-card">
      <%= link_to "+", new_vendor_path, id: "open-overlay", class: "add-link" %>
    </div>
  </div>
</div>

<div class="overlay" id="overlay" style="display: none;">
  <div class="overlay-content">
    <h1>Add New Vendor</h1>
    <!-- Dynamic content (the form) will be loaded here -->
    <div id="overlay-form-content">
      <!-- Placeholder for form -->
    </div>
    <button id="close-overlay">Cancel</button>
  </div>
</div>

<div class="footer">
  <div class="btn">
    <%= link_to "Equipment", equipments_path, class: "move-link" %>
  </div>
  <div class="btn">
    <%= link_to "Borrow", vendors_path, class: "move-link" %>
  </div>
  <div class="btn active">
    <%= link_to "Vendor", vendors_path, class: "move-link active" %>
  </div>
</div>