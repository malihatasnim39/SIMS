<div class="vendor-details">
  <div class="vendor-header">
    <h1 class="vendor-name"><%= @vendor.Name %></h1>
    <p class="vendor-phone">📞 <%= @vendor.Phone_Number %></p>
    <p class="vendor-address"><%= @vendor.Address %></p>
  </div>

  <div class="vendor-supplies">
    <h1>Supplies</h1>
    <ul class="supplies-list">
      <li>No items found in this group.</li>
    </ul>
  </div>

  <div class="vendor-actions">
    <%= link_to "Back", "#", onclick: "history.back(); return false;", class: "button back-button" %>
    <%= link_to "Update Vendor", edit_vendor_path(@vendor), id: "open-overlay", class: "button update-vendor" %>
    <%= button_to "Delete Vendor", vendor_path(@vendor), method: :delete, data: { confirm: "Are you sure?" }, class: "button delete-vendor" %>
  </div>
</div>

<div class="overlay" id="overlay" style="display: none;">
  <div class="overlay-content">
    <h1>Edit Vendor</h1>
    <!-- Dynamic content (the form) will be loaded here -->
    <div id="overlay-form-content">
      <!-- Placeholder for form -->
    </div>
    <button id="close-overlay">Cancel</button>
  </div>
</div>